import React from "react"
import Layout from "../components/Layout"

const resume = () => {
  return (
    <div>
      <a href="Sean_Lo_Resume.pdf">Download the file from the static folder</a>
    </div>
  )
}

export default resume
